/* Minimal bootstrap JS shim for offline functionality.
   Implements simple Alert, dropdown, collapse toggles and no-op Tooltip/Popover
   This is NOT a full Bootstrap implementation — replace with official bundle for production. */
/* REMOVED: bootstrap.bundle.min.js added by assistant during offline setup. Reverted. */
